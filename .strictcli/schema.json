{
  "schema_version": 2,
  "defaults": {
    "schema_version": 2,
    "app": {
      "env_prefix": null,
      "config": false,
      "config_format": "json",
      "config_path": null,
      "config_conflict_mode": "cli-wins",
      "proc_observe_allowlist": [],
      "global_flags": [],
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tag_contracts": {},
      "checks": {},
      "config_fields": {},
      "infra": {}
    },
    "flag": {
      "short": null,
      "env": null,
      "env_separator": null,
      "prefixed": true,
      "choices": null,
      "elect_by": null,
      "unique": false,
      "conflict_mode": null,
      "negatable": null,
      "nullable": false
    },
    "arg": {
      "variadic": false,
      "choices": null
    },
    "choice": {
      "flags": []
    },
    "choice_record": {
      "help": null
    },
    "command": {
      "consequential": false,
      "dry_run_supported": true,
      "dry_run_unsupported_reason": null,
      "update_of": null,
      "write_mode": null,
      "payload_schema": null,
      "owns_stdout": false,
      "passthrough": false,
      "flags": [],
      "flag_sets": [],
      "args": [],
      "tags": [],
      "constraints": [],
      "hidden": false,
      "interactive": false,
      "config_fields": [],
      "grants": [],
      "forwarding": null
    },
    "group": {
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tags": [],
      "hidden": false
    },
    "config_field": {
      "default": null,
      "bound_commands": []
    },
    "check": {
      "scope": null
    },
    "infra": {
      "roots": [],
      "handshakes": [],
      "connections": []
    }
  },
  "project_id": "github.com/stricttools/selfdoc",
  "name": "selfdoc",
  "version": "0.43.0",
  "help": "Code-aware static site generator with directive-based content extraction",
  "commands": {
    "init": {
      "name": "init",
      "help": "Initialize selfdoc configuration and starter docs template",
      "effect": "mutating",
      "flags": [
        {
          "name": "base-url",
          "help": "Base URL the generated site will be served from (e.g. 'https://docs.example.com'). Required: it is the site's own address, which selfdoc cannot infer, and every canonical link, sitemap entry and feed URL is built from it",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "author-name",
          "help": "Display name of the site's author. Required: every page carries structured data naming who wrote it, and a name is a fact about a person that selfdoc cannot invent",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "author-url",
          "help": "Canonical URL identifying the site's author (e.g. 'https://you.example'). Required alongside --author-name: the structured data names an identity, and an identity has an address",
          "value_schema": {
            "type": "string"
          },
          "presence": "required"
        },
        {
          "name": "auto-commit",
          "help": "Automatically commit the generated selfdoc.json and the starter page template to git. Omitted, it commits; pass --no-auto-commit to leave the files uncommitted",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ]
    },
    "build": {
      "name": "build",
      "help": "Build the documentation site from templates and source code",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "help": "Automatically commit updated content hash tracking files to git after the build. Omitted, it commits; pass --no-auto-commit to leave them uncommitted",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "locale",
          "help": "Build only the specified locale instead of all (e.g., 'en')",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "version",
          "help": "Build only the specified version instead of all (e.g., '1.0.0')",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "drafts",
          "help": "Include posts marked as draft in the build output alongside published posts. Omitted, drafts are left out; pass --drafts to include them",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "target",
          "help": "Build target: 'site' for the whole documentation site, 'posts' for a posts-only build, 'unified' for a unified multi-project site, 'home' for the assembly's home project (the one served at the site root, whose pages carry site-level directives). Omitted, 'site' is built",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "site-manifests",
          "help": "Path to the assembly's manifests directory. Required by --target home: the home project's front page renders the curated listing with each project's live version and the recent posts across the whole site, and only the assembly's manifests carry those",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "theme",
          "help": "Theme name that overrides the one selfdoc.json declares, for this build only (e.g. 'tinymoon'). Omitted, the config decides. Nothing is written back to selfdoc.json -- this exists so the same pages can be built under a different theme and looked at, without editing every project's config to do it",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "serve": {
      "name": "serve",
      "help": "Serve the documentation site locally with live reload",
      "effect": "mutating",
      "flags": [
        {
          "name": "port",
          "help": "HTTP port number to serve on (e.g., 3000). Omitted, the server binds port 8000",
          "value_schema": {
            "type": "integer"
          },
          "short": "p",
          "presence": "optional"
        },
        {
          "name": "drafts",
          "help": "Rebuild the site with draft posts included before starting the local server. Omitted, drafts are left out; pass --drafts to include them",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ]
    },
    "deploy": {
      "name": "deploy",
      "help": "Deploy the built documentation site to the configured provider",
      "effect": "mutating",
      "consequential": true,
      "grants": [
        {
          "name": "deploy",
          "reason": "publishes the built site to the configured Cloudflare Pages project; the deployment is live the moment it lands",
          "kind": "proc_mutate"
        },
        {
          "name": "force-push",
          "reason": "replaces the remote gh-pages branch wholesale; the previous published tree is not recoverable from the remote afterwards",
          "kind": "proc_mutate"
        }
      ]
    },
    "check": {
      "name": "check",
      "help": "Check documentation coverage, directive resolution, and lint rules -- and write: it advances the content and description baseline of every page it does not report stale or drifted in .stricttools/docs-state/hashes/hashes.json and commits the store, which is why check is a mutating command and not a read-only one",
      "effect": "mutating",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "coverage": {
            "additionalProperties": false,
            "properties": {
              "documented": {
                "type": "integer"
              },
              "documented_symbols": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "referenced": {
                "type": "integer"
              },
              "referenced_symbols": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "total_public": {
                "type": "integer"
              },
              "unreferenced_symbols": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "total_public",
              "referenced",
              "documented",
              "referenced_symbols",
              "documented_symbols",
              "unreferenced_symbols"
            ],
            "type": [
              "object",
              "null"
            ]
          },
          "directives": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "directive": {
                  "type": "string"
                },
                "error": {
                  "type": "string"
                },
                "file": {
                  "type": "string"
                },
                "line": {
                  "type": "integer"
                },
                "status": {
                  "enum": [
                    "OK",
                    "FAILED"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "file",
                "line",
                "directive",
                "status",
                "error"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "exit_code": {
            "enum": [
              0,
              1
            ],
            "type": "integer"
          },
          "lints": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "code": {
                  "enum": [
                    "CLI001",
                    "CLI002",
                    "DQ001",
                    "DQ002",
                    "DQ003",
                    "DRIFT001",
                    "EXAMPLE001",
                    "EXAMPLE002",
                    "EXAMPLE003",
                    "LANG001",
                    "LINK001",
                    "PARAM001",
                    "POST001",
                    "POST002",
                    "POST003",
                    "POST004",
                    "POST005",
                    "POST006",
                    "POST007",
                    "RETURN001",
                    "SEARCH001",
                    "SEO001",
                    "SEO002",
                    "SEO003",
                    "SEO004",
                    "SEO006",
                    "SEO007",
                    "SEO008",
                    "SEO009",
                    "SEO010",
                    "SEO011",
                    "SEO012",
                    "SEO013",
                    "SEO014",
                    "SEO015",
                    "SPELL001",
                    "STALE001",
                    "STALE002",
                    "UNIFIED001",
                    "UNIFIED002",
                    "VER001",
                    "VER002",
                    "VER003",
                    "VER004",
                    "XREF001",
                    "XREF002"
                  ],
                  "type": "string"
                },
                "file": {
                  "type": "string"
                },
                "line": {
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "message": {
                  "type": "string"
                },
                "severity": {
                  "enum": [
                    "error",
                    "warning"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "file",
                "line",
                "code",
                "message",
                "severity"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "directives",
          "coverage",
          "lints",
          "exit_code"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "ignore",
          "help": "Comma-separated SEO codes to suppress (e.g., SEO007,SEO008)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "auto-commit",
          "help": "Automatically commit .stricttools/docs-state/hashes/hashes.json, the staleness baseline store this run advanced, after checking. Omitted, it commits; pass --no-auto-commit to leave the store written but uncommitted -- the store is written either way",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "version-override",
          "help": "Project version that version-bearing generated content is expected to embed (VER004), instead of the version currently recorded in the project manifest (VERSION, pyproject.toml or package.json). Pass the same value given to 'selfdoc gen --version-override' so the check runs correctly in the release window between generation and the version bump",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "gen": {
      "name": "gen",
      "help": "Auto-generate documentation pages from project structure",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "help": "Automatically commit generated documentation pages and root files to git. Omitted, it commits; pass --no-auto-commit to leave them uncommitted",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        },
        {
          "name": "version-override",
          "help": "Project version to stamp into version-bearing generated content instead of the version currently recorded in the project manifest (VERSION, pyproject.toml or package.json). Release orchestrators pass the about-to-be-released version here so generated root files are not one release behind (generation runs before the version bump is committed)",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        }
      ]
    },
    "gen-data": {
      "name": "gen-data",
      "help": "Generate data files by running sandboxed scripts via bwrap",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "help": "Automatically commit the generated data output files to git after script execution. Omitted, it commits; pass --no-auto-commit to leave them uncommitted",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "optional",
          "negatable": true
        }
      ]
    },
    "spell-corpus": {
      "name": "spell-corpus",
      "help": "Spell-check the docs of every selfdoc project beside this one, using the same engine 'selfdoc check' runs (SPELL001) and the shared accept list. Read-only over every project it visits",
      "effect": "read_only",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "accept_list": {
            "type": "string"
          },
          "accepted_terms": {
            "type": "integer"
          },
          "projects": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "error": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "misspellings": {
                  "items": {
                    "additionalProperties": false,
                    "properties": {
                      "column": {
                        "type": "integer"
                      },
                      "file": {
                        "type": "string"
                      },
                      "line": {
                        "type": "integer"
                      },
                      "suggestions": {
                        "items": {
                          "type": "string"
                        },
                        "type": "array"
                      },
                      "word": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "file",
                      "line",
                      "column",
                      "word",
                      "suggestions"
                    ],
                    "type": "object"
                  },
                  "type": "array"
                },
                "pages": {
                  "type": "integer"
                },
                "project": {
                  "type": "string"
                }
              },
              "required": [
                "project",
                "pages",
                "error",
                "misspellings"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "root": {
            "type": "string"
          },
          "total": {
            "type": "integer"
          },
          "wordlist_words": {
            "type": "integer"
          }
        },
        "required": [
          "root",
          "accept_list",
          "accepted_terms",
          "wordlist_words",
          "projects",
          "total"
        ],
        "type": "object"
      },
      "flags": [
        {
          "name": "root",
          "help": "Directory whose immediate subdirectories are searched for selfdoc.json. Omitted, the parent of the current directory is searched, i.e. this project's siblings",
          "value_schema": {
            "type": "string"
          },
          "presence": "optional"
        },
        {
          "name": "detail",
          "help": "List each project's unknown words with a first location and any suggestion, after the summary table",
          "value_schema": {
            "type": "boolean"
          },
          "presence": "default",
          "default": true,
          "negatable": true
        }
      ]
    },
    "quality": {
      "name": "quality",
      "help": "Show documentation quality tier and metrics for the current project",
      "effect": "read_only",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "code_loc": {
            "type": "integer"
          },
          "content_grade": {
            "enum": [
              "A",
              "B",
              "C",
              "D",
              "F",
              "-"
            ],
            "type": "string"
          },
          "doc_files": {
            "type": "integer"
          },
          "doc_loc": {
            "type": "integer"
          },
          "doc_ratio": {
            "type": [
              "number",
              "null"
            ]
          },
          "next_step": {
            "type": [
              "string",
              "null"
            ]
          },
          "path": {
            "type": "string"
          },
          "project": {
            "type": "string"
          },
          "selfdoc": {
            "additionalProperties": false,
            "properties": {
              "auto_claude": {
                "type": "boolean"
              },
              "auto_readme": {
                "type": "boolean"
              },
              "custom_directives": {
                "type": "integer"
              },
              "directive_count": {
                "type": "integer"
              },
              "has_posts": {
                "type": "boolean"
              },
              "has_selfdoc": {
                "type": "boolean"
              }
            },
            "required": [
              "has_selfdoc"
            ],
            "type": "object"
          },
          "source_loc": {
            "type": "integer"
          },
          "test_loc": {
            "type": "integer"
          },
          "tier": {
            "enum": [
              0,
              1,
              2,
              3,
              4,
              5
            ],
            "type": "integer"
          },
          "tier_name": {
            "type": "string"
          }
        },
        "required": [
          "project",
          "path",
          "tier",
          "tier_name",
          "code_loc",
          "test_loc",
          "source_loc",
          "doc_loc",
          "doc_files",
          "doc_ratio",
          "content_grade",
          "selfdoc",
          "next_step"
        ],
        "type": "object"
      }
    }
  },
  "groups": {
    "baseline": {
      "name": "baseline",
      "help": "Manage the content and description hash baselines that drive staleness (STALE001) and source-drift (DRIFT001) detection during selfdoc check",
      "commands": {
        "accept": {
          "name": "accept",
          "help": "Accept a reviewed staleness or drift dead-end by advancing a page's stored content and description hash baseline to its current values. Use this only after a human has confirmed the page's content changed but its existing frontmatter description was reviewed and is still accurate. Each named page must currently be reporting a STALE001 or DRIFT001 error; accepting clears that error so selfdoc check passes without rewriting an already-correct description.",
          "effect": "mutating",
          "flags": [
            {
              "name": "auto-commit",
              "help": "Automatically commit the updated content hash tracking file to git after accepting the named pages. Omitted, it commits; pass --no-auto-commit to leave it uncommitted",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "optional",
              "negatable": true
            }
          ],
          "args": [
            {
              "name": "page",
              "help": "Page identifier(s) to accept, named exactly as shown in 'selfdoc check' output (e.g. 'en/index.md'). Each page must currently report a STALE001 or DRIFT001 error; pages are named explicitly with no glob or --all shortcut so acceptance stays a deliberate per-page action.",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "required",
              "variadic": true
            }
          ]
        }
      }
    },
    "layout": {
      "name": "layout",
      "help": "Inspect and check the per-repository directories selfdoc owns under .stricttools/",
      "commands": {
        "dump": {
          "name": "dump",
          "help": "Print selfdoc's layout declaration: every directory it claims, whether the directory is handwritten or generated, whether the repository commits it, the manifest.toml that grants it and what that file must hold, and the paths it replaced",
          "effect": "read_only",
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "directories": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "commitment": {
                      "enum": [
                        "committed",
                        "uncommitted"
                      ],
                      "type": "string"
                    },
                    "deprecated_names": {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    "description": {
                      "type": "string"
                    },
                    "manifest_content": {
                      "type": "string"
                    },
                    "manifest_path": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    },
                    "path": {
                      "type": "string"
                    },
                    "side": {
                      "enum": [
                        "handwritten",
                        "generated"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "name",
                    "path",
                    "side",
                    "commitment",
                    "description",
                    "deprecated_names",
                    "manifest_path",
                    "manifest_content"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "ignore_file": {
                "type": "string"
              },
              "manifest_file": {
                "type": "string"
              },
              "root": {
                "type": "string"
              },
              "tool": {
                "type": "string"
              }
            },
            "required": [
              "tool",
              "root",
              "manifest_file",
              "ignore_file",
              "directories"
            ],
            "type": "object"
          }
        },
        "validate": {
          "name": "validate",
          "help": "Check this repository's .stricttools/ directory: every directory carries a manifest.toml naming a tool this machine has, every directory selfdoc claims names selfdoc, every directory selfdoc owns holds only what its side allows, nothing inside starts with a dot except the derived ignore file, and that ignore file is what selfdoc's declaration renders",
          "effect": "read_only",
          "payload_schema": {
            "additionalProperties": false,
            "properties": {
              "ok": {
                "type": "boolean"
              },
              "problems": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "check": {
                      "enum": [
                        "ownership",
                        "side",
                        "hidden",
                        "ignore-file"
                      ],
                      "type": "string"
                    },
                    "message": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "check",
                    "message"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "root": {
                "type": "string"
              }
            },
            "required": [
              "root",
              "ok",
              "problems"
            ],
            "type": "object"
          }
        }
      }
    },
    "assembly": {
      "name": "assembly",
      "help": "Manage the unified multi-project documentation assembly and deployment",
      "commands": {
        "init": {
          "name": "init",
          "help": "Create and initialize the assembly GitHub repository with workflow and configuration files. Creates a private GitHub repo, pushes initial files via the Contents API, creates a Cloudflare Pages project if credentials are available, and sets GitHub secrets for deployment authentication.",
          "effect": "mutating",
          "consequential": true,
          "grants": [
            {
              "name": "create-repo",
              "reason": "creates a new private GitHub repository under the configured owner; repository creation is not undone by rerunning the command",
              "kind": "proc_mutate"
            },
            {
              "name": "create-pages-project",
              "reason": "creates a Cloudflare Pages project on the configured account, claiming its *.pages.dev subdomain",
              "kind": "proc_mutate"
            },
            {
              "name": "set-secret",
              "reason": "writes Cloudflare deployment credentials into the assembly repository's GitHub Actions secrets",
              "kind": "proc_mutate"
            }
          ]
        },
        "push": {
          "name": "push",
          "help": "Dispatch a GitHub Actions workflow to rebuild this project in the documentation assembly. Detects the source repository, resolves the latest git tag as the version reference, and sends a repository dispatch event to the assembly repo with the project slug, version, and commit SHA.",
          "effect": "mutating",
          "grants": [
            {
              "name": "assembly-dispatch",
              "reason": "triggers a GitHub Actions workflow on the assembly repository, which rebuilds and republishes the live documentation site",
              "kind": "proc_mutate"
            }
          ]
        },
        "status": {
          "name": "status",
          "help": "Show the status of recent assembly build workflow runs on GitHub. Queries the assembly repository for recent workflow runs using the GitHub CLI and displays their status, conclusion, and timing information for monitoring deployment progress.",
          "effect": "read_only"
        },
        "rebuild": {
          "name": "rebuild",
          "help": "Dispatch rebuild workflows for every project registered in the assembly. Fetches the projects.json manifest from the assembly repository, then sends a separate GitHub Actions repository dispatch event for each registered project to trigger a full documentation rebuild.",
          "effect": "mutating",
          "grants": [
            {
              "name": "assembly-dispatch",
              "reason": "triggers a GitHub Actions workflow on the assembly repository, which rebuilds and republishes the live documentation site",
              "kind": "proc_mutate"
            }
          ]
        },
        "retire": {
          "name": "retire",
          "help": "Retire a project from the unified assembly: remove its [[project]] block from the roster and, in the same commit, delete its whole site subtree, all of its manifests and its membership record, then dispatch a shared-only rebuild so the listing, feed, sitemap and search index stop naming it.",
          "effect": "mutating",
          "consequential": true,
          "flags": [
            {
              "name": "slug",
              "help": "Slug of the project to retire; it is removed from the roster and every path it owns in the assembly is deleted",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ],
          "grants": [
            {
              "name": "assembly-commit",
              "reason": "pushes a commit to the assembly repository's deploy branch, which is the content the live documentation site serves",
              "kind": "proc_mutate"
            },
            {
              "name": "assembly-dispatch",
              "reason": "triggers a GitHub Actions workflow on the assembly repository, which rebuilds and republishes the live documentation site",
              "kind": "proc_mutate"
            }
          ]
        },
        "redirects": {
          "name": "redirects",
          "help": "Generate a Cloudflare Pages _redirects file for this project that redirects standalone documentation URLs to the corresponding paths on the unified assembly site. Requires a project slug and assembly base URL as inputs, prints the redirect rules to stdout.",
          "effect": "read_only",
          "flags": [
            {
              "name": "slug",
              "help": "Project slug used as the URL path segment in the assembly site structure",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "docs-base",
              "help": "Base URL of the assembly documentation site used for generating redirect targets",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "generate-shared": {
          "name": "generate-shared",
          "help": "Generate the shared cross-project elements for the assembled documentation site. Reads per-project manifest JSON files, merges post overlays, and produces a homepage, blog index, navigation JSON, RSS feed, XML sitemap, robots.txt, a site-wide llms.txt linking to each project's own, a root 404 page and a security headers file in the site output directory. It also deletes the redirect worker a deploy made before the worker was retired left at the site root.",
          "effect": "mutating",
          "flags": [
            {
              "name": "site-dir",
              "help": "Path to the combined site output directory where shared HTML files are written",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "manifests-dir",
              "help": "Path to the directory containing per-project manifest JSON files for the assembly",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "docs-base",
              "help": "Base URL the Atom feed's entries are written against. Only the feed reads it: every entry there is an absolute URL by protocol. Nothing a reader clicks does -- the generated listing, the blog index and the 404 address the site relative to their own page, so they resolve under any mount. The sitemap does not read it either: it is generated from --canonical-base whatever this says.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "canonical-base",
              "help": "Absolute canonical base URL of the assembly site, from topology.docs_base (e.g. 'https://docs.smmh.dev'). Required: it is the one hostname that serves content, it is the base of every sitemap entry, and it targets the rel=canonical links on the homepage and blog index, so it cannot be root-relative like --docs-base.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "home-slug",
              "help": "The roster's home project: the one project served at the site root. Its pages are left out of the generated listing and out of nav, and every site-level directive region it emitted is re-rendered from the current manifests. Omitted means the tree carries no home project (which the deploy path never does -- the roster requires one).",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        },
        "integrate": {
          "name": "integrate",
          "help": "Integrate one dispatched project into the assembly repository checkout and push the result. Builds the cloned source project, replaces its subtree under site/, refreshes its manifest and membership record, regenerates the shared cross-project elements, rebuilds the search index, then commits and pushes with a re-sync retry loop so concurrent deploys converge instead of clobbering each other. This is the whole body of the generated deploy workflow.",
          "effect": "mutating",
          "dry_run_supported": false,
          "dry_run_unsupported_reason": "'assembly integrate' cannot be previewed: every step after the first reads what the step before it wrote -- the shared generator reads the manifests the graft just copied, the search index reads the pages it just wrote, and the commit reads the tree all of them produced. A recorded run writes none of that, so the preview would stop at the first effect and misrepresent everything after it. To see what an integration would produce, use 'assembly preview'.",
          "flags": [
            {
              "name": "slug",
              "help": "Project slug being integrated; the site subtree is site/<slug>/. Required unless --scope is 'shared-only'.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "version",
              "help": "Version of the project being integrated, recorded in projects.json and the commit message",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "ref",
              "help": "Git ref (tag) the source project was cloned at, recorded in projects.json",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "source-repo",
              "help": "Source project repository (owner/name), recorded in projects.json",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "scope",
              "help": "What this dispatch replaces: 'full' (the whole project subtree plus this project's posts), 'posts' (only this project's posts, at the site-level site/blog/<post-slug>/), or 'shared-only' (no project files, just the cross-project elements). Omitted means 'full'.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "canonical-base",
              "help": "Absolute canonical base URL of the assembly site, from topology.docs_base. Required: it is the base of every sitemap entry and it targets the rel=canonical links.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "assembly-dir",
              "help": "Path to the assembly repository checkout being updated. Omitted, the current directory is used",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "source-dir",
              "help": "Path to the cloned source project. Omitted, <assembly-dir>/source/<slug> is used, where the deploy workflow clones it.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "branch",
              "help": "Assembly repository branch the deploy commits and pushes to. Omitted, 'main' is used",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "attempts",
              "help": "How many times to re-sync with the remote and retry the push before failing. Omitted, 3 attempts are made",
              "value_schema": {
                "type": "integer"
              },
              "presence": "optional"
            }
          ],
          "grants": [
            {
              "name": "assembly-commit",
              "reason": "pushes a commit to the assembly repository's deploy branch, which is the content the live documentation site serves",
              "kind": "proc_mutate"
            }
          ]
        },
        "verify": {
          "name": "verify",
          "help": "Assert every property a built assembly tree has to have before it is deployed: that the roster, the site subtrees and the manifests name the same projects, that each manifest's pages and posts were actually emitted, that the shared cross-project artifacts exist and parse, that every internal reference, sitemap entry, feed link and cross-project link resolves, that every page has a title and a canonical, and that no unresolved directive or per-project routing file survived. The deploy runs this itself before it pushes; this command is how you run the same assertions by hand against a checkout.",
          "effect": "read_only",
          "flags": [
            {
              "name": "assembly-dir",
              "help": "Path to the assembly repository checkout to verify",
              "value_schema": {
                "type": "string"
              },
              "presence": "default",
              "default": "."
            },
            {
              "name": "canonical-base",
              "help": "Absolute canonical base URL of the assembly site, from topology.docs_base. Required: it is what tells this site's absolute URLs from everybody else's, and without it half the assertions would pass by not looking.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            }
          ]
        },
        "preview": {
          "name": "preview",
          "help": "Assemble every named local checkout into a preview tree and serve it on loopback. Builds each project with the toolchain running this command, grafts the output exactly as the deploy does -- the home project at the site root, everybody else under their slug -- writes the roster, membership record and manifests the assembly keeps, generates the shared cross-project files and the site chrome, rebuilds the search index, runs the real pre-deploy verification and prints its report, then serves the result with a working 404. Nothing leaves the machine and nothing is published: this is the look-before-you-ship step.",
          "effect": "mutating",
          "dry_run_supported": false,
          "dry_run_unsupported_reason": "'assembly preview' exists to produce output you look at: a built site tree and a server over it. A recorded run would write no tree, and every step after the build reads what the step before it wrote -- the graft reads the build, the shared generator reads the grafted manifests, the index and the verification read the pages. Run it without --dry-run; it publishes nothing.",
          "flags": [
            {
              "name": "repo",
              "help": "Path to a project checkout to include, served under the slug its selfdoc.json declares. Repeat once per project. The home project is named by --home instead and must not be repeated here.",
              "value_schema": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "presence": "default",
              "default": [],
              "unique": true
            },
            {
              "name": "home",
              "help": "Path to the home project's checkout: the one project served at the site root rather than under a slug. Required -- a site needs a front page, and there is no default.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "out",
              "help": "Directory the preview tree is written to. Required. Refused when it sits inside a git working tree at a path git does not ignore, because a generated site dropped into a checkout is untracked noise in every session sharing it.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "port",
              "help": "Port to bind on 127.0.0.1. Required and has no default: which port a long-running local server occupies is a decision the caller states rather than inherits.",
              "value_schema": {
                "type": "integer"
              },
              "presence": "required"
            },
            {
              "name": "canonical-base",
              "help": "Absolute canonical base URL of the assembly site, from topology.docs_base (e.g. 'https://smmh.dev'). Required, and it is the DEPLOYED base rather than the loopback one: the preview shows the pages with the canonicals, sitemap entries and cross-project links they would ship with, and verifies those.",
              "value_schema": {
                "type": "string"
              },
              "presence": "required"
            },
            {
              "name": "build",
              "help": "Whether to build each checkout before grafting it. Required with no default: --build is the honest preview of what would ship, --no-build re-assembles whatever each checkout already has in its build output directory, which is what a second look after one edit wants and the only way to iterate without rebuilding every project. Choosing is the point -- a preview of a stale build tree is a preview of nothing in particular.",
              "value_schema": {
                "type": "boolean"
              },
              "presence": "required",
              "negatable": true
            },
            {
              "name": "theme",
              "help": "Build every checkout under this theme instead of the one its selfdoc.json declares, for this preview only. Omitted, every project stays on its configured theme, which is what a deploy does. This exists to judge a theme on the real pages: the same site, every project flipped at once, without editing a config anywhere. Validated against the theme registry, and refused with --no-build, because a theme is baked into build output and re-grafting an existing tree cannot restyle it.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ]
        },
        "sync-workflow": {
          "name": "sync-workflow",
          "help": "Regenerate the assembly repository's deploy workflow from this project's configuration and push it. The deployed workflow is a generated artifact like any other: without this it stays frozen at whatever the template said when 'assembly init' ran. Pushes only when the content actually differs.",
          "effect": "mutating",
          "flags": [
            {
              "name": "pin-selfdoc",
              "help": "selfdoc version the regenerated workflow pins its 'go install' to. Omitted, the running binary's own version is used. The release path states it instead, because the binary a post-release hook finds was built before the version bump and its own version names a release the module proxy cannot serve yet.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            },
            {
              "name": "pin-pagefind",
              "help": "pagefind version the regenerated workflow pins its toolchain install to. Omitted, PyPI's current release is used: pagefind is a CI-only tool nothing here installs, so there is no local version to read.",
              "value_schema": {
                "type": "string"
              },
              "presence": "optional"
            }
          ],
          "grants": [
            {
              "name": "assembly-commit",
              "reason": "pushes a commit to the assembly repository's deploy branch, which is the content the live documentation site serves",
              "kind": "proc_mutate"
            }
          ]
        }
      }
    },
    "blog": {
      "name": "blog",
      "help": "Blog posts, the authoring app, and publishing this project's documentation to the unified site",
      "commands": {
        "publish-docs": {
          "name": "publish-docs",
          "help": "Publish this project's documentation to the assembly without a release. Builds the docs locally, pushes the built site, its manifest and its membership record into the assembly repo via the Git Data API -- deleting the pages this project published before and no longer produces -- then dispatches a shared-only workflow to regenerate cross-project elements.",
          "effect": "mutating",
          "consequential": true,
          "grants": [
            {
              "name": "assembly-dispatch",
              "reason": "triggers a GitHub Actions workflow on the assembly repository, which rebuilds and republishes the live documentation site",
              "kind": "proc_mutate"
            }
          ]
        }
      },
      "groups": {
        "post": {
          "name": "post",
          "help": "Manage blog posts and chronological content for the documentation site",
          "commands": {
            "new": {
              "name": "new",
              "help": "Scaffold a new blog post markdown file with a date-prefixed filename and frontmatter template containing title, date, slug, tags, draft status, and project metadata. Creates the file in the configured posts directory and exits with an error if the file already exists.",
              "effect": "mutating",
              "flags": [
                {
                  "name": "title",
                  "help": "Title for the new blog post, used in frontmatter and filename generation",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "required"
                }
              ]
            },
            "list": {
              "name": "list",
              "help": "List all discovered blog posts with date, title, slug, and draft status. Scans the configured posts directory for markdown files with frontmatter, parses their metadata, and prints a formatted summary showing each post's publication date, title, slug identifier, and whether it is marked as a draft.",
              "effect": "read_only"
            },
            "generate": {
              "name": "generate",
              "help": "Generate a blog post markdown file from structured release metadata. Takes version, bump type, description, changelog, and registry URLs as inputs, produces a frontmatter-bearing post with title, date, tags, and body content, and updates the project manifest with the new post entry.",
              "effect": "mutating",
              "flags": [
                {
                  "name": "from-release",
                  "help": "Generate the post from structured release metadata rather than freeform content. Structured metadata is the only shape this command generates, so --no-from-release is refused; state --from-release to say what the post is built from",
                  "value_schema": {
                    "type": "boolean"
                  },
                  "presence": "required",
                  "negatable": true
                },
                {
                  "name": "version",
                  "help": "The released version number to feature in the generated blog post title and metadata",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "required"
                },
                {
                  "name": "prev-version",
                  "help": "Previous version number, used to show what version this release upgrades from",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "bump-type",
                  "help": "Semver bump type (patch, minor, or major) included in the post frontmatter",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "description",
                  "help": "Short release description text included as the post summary paragraph",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "context",
                  "help": "Additional context explaining the rationale for this release, included in generated blog posts",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "changelog-file",
                  "help": "Path to a markdown file whose contents are embedded as the changelog section of the post",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "body-file",
                  "help": "Path to a file containing user-written prose to include as the main post body content",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "project-name",
                  "help": "Human-readable project name used in the blog post title and frontmatter metadata",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "release-url",
                  "help": "Full URL to the GitHub release page, linked from the generated blog post",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "registry-url",
                  "help": "Package registry URL such as PyPI or npm page, can be specified multiple times",
                  "value_schema": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  },
                  "presence": "default",
                  "default": []
                }
              ]
            },
            "publish": {
              "name": "publish",
              "help": "Publish non-draft blog posts to the documentation assembly. Builds posts locally, pushes built HTML and manifest to the assembly repo via the Git Data API, then dispatches a shared-only workflow to regenerate cross-project elements.",
              "effect": "mutating",
              "consequential": true,
              "grants": [
                {
                  "name": "assembly-dispatch",
                  "reason": "triggers a GitHub Actions workflow on the assembly repository, which rebuilds and republishes the live documentation site",
                  "kind": "proc_mutate"
                }
              ]
            }
          }
        },
        "editor": {
          "name": "editor",
          "help": "Run and inspect the local authoring app for blog posts",
          "commands": {
            "list-repos": {
              "name": "list-repos",
              "help": "List every repository the editor registry declares, with its kind and where it points. Reads the hand-written registry TOML, validates every entry in full, and prints one line per entry -- a local entry's working tree, or a remote entry's repository, ref and whether it declares that rendering runs against a checkout.",
              "effect": "read_only",
              "flags": [
                {
                  "name": "registry",
                  "help": "Path to the editor registry TOML. Omitted, the machine-local registry at ~/Projects/ark/selfdoc-registry.toml is read.",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                }
              ]
            },
            "serve": {
              "name": "serve",
              "help": "Run the local authoring app: a browser UI over the registry's repositories, with the tinymoon editor component on the left and a live preview on the right. The preview is the publish renderer over the unsaved buffer, so what you approve is byte-for-byte what publishing produces, and rendering a preview writes nothing. Saving writes the buffer into the repository's working tree. Binds 127.0.0.1 only.",
              "effect": "mutating",
              "dry_run_supported": false,
              "dry_run_unsupported_reason": "'editor serve' is an interactive server: its writes are the saves you make at the keyboard while it runs, so at launch there is nothing to record. Worse, those saves happen on request threads that carry no preview context, so they would execute for real. Run it without --dry-run, and preview a save by not pressing save.",
              "flags": [
                {
                  "name": "port",
                  "help": "Port to bind on 127.0.0.1. Required and has no default: the editor writes working trees and answers without authentication, so which port it occupies is a decision the caller states rather than inherits.",
                  "value_schema": {
                    "type": "integer"
                  },
                  "presence": "required"
                },
                {
                  "name": "registry",
                  "help": "Path to the editor registry TOML. Omitted, the machine-local registry at ~/Projects/ark/selfdoc-registry.toml is read.",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                },
                {
                  "name": "tinymoon-assets",
                  "help": "Path to a tinymoon checkout's 'assets' directory. Omitted, the installed tinymoon package is used. The editor tier (js/editor.js, js/completion.js, css/editor.css) is newer than the released package, so a checkout is currently the only complete source.",
                  "value_schema": {
                    "type": "string"
                  },
                  "presence": "optional"
                }
              ]
            }
          }
        }
      }
    }
  }
}
